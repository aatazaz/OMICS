# OMICS
This Study critically examines the hypothesis that transformer and large language model (LLM) architectures achieve superior predictive performance when augmented with structured knowledge graph signal, using TxGNN, a graph neural network (GNN) trained on PrimeKG for zeroshot drug repurposing, as the central case study.
